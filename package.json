{
  "name": "reform",
  "private": true,
  "scripts": {
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream",
    "storybook": "lerna run storybook --stream"
  },
  "dependencies": {
    "lerna": "^8.1.2"
  }
}
