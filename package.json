{
    "name": "reform",
    "private": true,
    "scripts": {
      "build": "rush build",
      "storybook": "rush storybook --verbose",
      "deploy:build": "rush build",
      "deploy:publish": "rush publish --publish  --apply --target-branch main --set-access-level public --registry https://registry.npmjs.org/",
      "deploy": "npm run deploy:build || npm run deploy:publish"
    }
}
