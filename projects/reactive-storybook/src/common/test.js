/*

function useSmartCallback(callback, deps) {
    const callback useCallback(callback, deps);

    return {
        use: (meta) => {
            
        };
    }
}

const handleClick = useSmartCallback(() => {}, []);

map(list, (item, index) => {
    <div onClick={() => {}} onClick={handleClick.use(index)}>
})

*/