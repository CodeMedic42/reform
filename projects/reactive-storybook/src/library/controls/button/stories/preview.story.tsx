import React from 'react';
import Button from '@reformjs/reactive/controls/button';

export default function DefaultStory(props: Record<string, unknown>) {
    return (
        <Button {...props} />
    );
}
